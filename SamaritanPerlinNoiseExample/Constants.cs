using System;

namespace SamaritanPerlinNoise
{
    public static class Constants
    {
        public const int ImageWidth = 400;       // Width of the image.
        public const int ImageHeight = 200;      // Height of the image.
        public const double NoiseScale = 0.05;   // Scale factor for adjusting noise frequency.
        public const string OutputFileName = "perlin_noise.png";  // Output image file name.
    }
}
